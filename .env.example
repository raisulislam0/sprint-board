## Vite dev proxy target (your Frappe site)
# VITE_FRAPPE_PROXY_TARGET=http://support:8000

## If your bench is multi-site, set the Frappe site host (Host header)
## This fixes: "localhost does not exist" (404) from Frappe.
# VITE_FRAPPE_SITE_HOST= support

